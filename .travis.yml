dist: xenial
language: ruby
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install texlive-full
script:
  - if ! git diff --name-only $TRAVIS_COMMIT_RANGE | grep -qvE 'dartLangSpec.tex'
  - then
    - echo "dartLangSpex.tex wasn't changed; skipping spec PDF build"
    - exit 0
  - else
    - cd specification
    - make
  - fi
